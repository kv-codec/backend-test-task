# yaml-language-server: $schema=https://json.schemastore.org/lefthook.json
pre-commit:
    commands:
        unit-tests:
            run: mise 'tests:**unit'

pre-push:
    commands:
        all-tests:
            run: mise 'tests:**'
